// Declaración de un arreglo con distintas edades
let edades = [12, 18, 25, 10, 30, 16];

// Filtra las edades que son mayores o iguales a 18
let mayores = edades.filter((edad) => edad >= 18);

// Muestra en consola las edades filtradas
console.log(mayores); // [18, 25, 30]
